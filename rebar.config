{erl_opts, [{i, include}]}.

{so_name, "ham_nifs.so"}.
{port_envs, [{"DRV_CFLAGS", "$DRV_CFLAGS -g -Wall -Ic_src -Ic_src/include"},
             {"DRV_LDFLAGS", "$DRV_LDFLAGS c_src/libhamsterdb.a -lboost_system -lpthread -ldl"}]}.

{port_pre_script, {"make -C c_src", ""}}.
{port_cleanup_script, "make -C c_src clean"}.
